<UserControl x:Class="SalmaMusic.MVVM.Views.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SalmaMusic.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
            <Button Content="Reset" Command="{Binding DeleteCanvasContent}" Click="Button_Click" Width="100"/>
            <Button Content="Save" Click="Button_Click_1" Width="100"/>
            <Button Content="Reset" Width="100"/>
            <ComboBox x:Name="ColorPicker" SelectedItem="{Binding SelectedColorItem}" SelectionChanged="ColorPicker_SelectionChanged" ItemsSource="{Binding Colors}" DisplayMemberPath="DisplayName"/>
        </StackPanel>

        <Canvas x:Name="MyCanvas" MouseMove="MyCanvas_MouseMove" Background="Black" Height="500" MouseDown="Canvas_MouseDown">
            <!--<Ellipse Width="200" Height="200" Fill="Blue" Canvas.Left="100" Canvas.Top="{Binding EllipseJumpingFrequency}" />-->
            <!-- Add more elements here -->
        </Canvas>
    </StackPanel>

</UserControl>
